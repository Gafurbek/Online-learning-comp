const menuBtn = document.querySelector('.menu__btn'),
    closeBtn = document.querySelector('.close__btn'),
    menu = document.querySelector('.nav__link');


menuBtn.addEventListener('click', event => menu.style.transform = 'translateY(0)');

closeBtn.addEventListener('click', event => menu.style = clientInformation)
